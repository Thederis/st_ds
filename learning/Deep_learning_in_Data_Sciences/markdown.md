 f(x) = \begin(cases) x     если  x > 0, 
    α * (e^x - 1)     если  x ≤ 0, \end(cases)

“`latex
   f(x) = \begin(cases) x     если  x > 0, 
 α·(e^x - 1)     если  x ≤0, \end(cases)
   “`

f(x) = \begin{cases} 
            x & \text{если } x > 0, \\
            \alpha \cdot (e^x - 1) & \text{если } x \leq 0
        \end{cases}